'use strict'

/**
 * Get all files matching regex in folder
 * @param {string} source the path to search
 * @param {string} match the string to match
 */
var getFiles = function(source, match) {
  console.log('source: ', source)
  console.log('match: ', match)
}

module.exports = getFiles
